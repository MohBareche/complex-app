<%- include("includes/header") %>

    <div class="container py-md-2">
      <% errors.forEach((message) => { %>
      <div class="alert alert-danger text-center"><%= message %></div>
      <% }) %>

      <div class="row align-items-center">
        <div class="col-lg-7 py-3 py-md-5">
          <h1 class="display-3">Vous vous souvenez de l'écriture?</h1>
          <p class="lead text-muted">
            Vous en avez assez des tweets courts et des messages
            &ldquo;partagés&rdquo; impersonnels qui rappellent les transferts
            d'e-mails de la fin des années 90? Nous pensons que revenir à
            l'écriture est la clé pour profiter à nouveau d'Internet.
          </p>
        </div>
        <div class="col-lg-5 pl-lg-5 pb-3 py-lg-5">
          <form action="/register" method="POST" id="registration-form">
            <% regErrors.forEach((message) => { %>
            <div class="alert alert-danger small"><%= message %></div>
            <% }) %>

            <div class="form-group">
              <label for="username-register" class="text-muted mb-1"
                ><small>Utilisateur</small></label
              >
              <input
                name="username"
                id="username-register"
                class="form-control"
                type="text"
                placeholder="votre nom utilisateur"
                autocomplete="off"
              />
            </div>

            <div class="form-group">
              <label for="email-register" class="text-muted mb-1"
                ><small>Email</small></label
              >
              <input
                name="email"
                id="email-register"
                class="form-control"
                type="text"
                placeholder="votre-email@exemple.com"
                autocomplete="off"
              />
            </div>

            <div class="form-group">
              <label for="password-register" class="text-muted mb-1"
                ><small>Mot de passe</small></label
              >
              <input
                name="password"
                id="password-register"
                class="form-control"
                type="password"
                placeholder="entrez un mot de passe"
              />
            </div>

            <button
              type="submit"
              class="py-3 mt-4 btn btn-lg btn-success btn-block"
            >
              Inscrivez-vous
            </button>
            <p class="text-muted">
              Si vous n'avez pas de compte, inscrivez-vous ! c'est gratuit
            </p>
          </form>
        </div>
      </div>
    </div>

<%- include('includes/footer') %>